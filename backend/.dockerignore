# Serving artifacts come from the Railway Volume at runtime, not the image.
chroma_db/
graph_data/
vector_taxonomy.json

# Query log — prod's lives on the Volume; never bake a local one into the image
query_logs.db*

# Secrets + local cruft
.env
__pycache__/
*.pyc

Dockerfile
.dockerignore
